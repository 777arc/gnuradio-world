// Bundle entry for validation.test.mjs: the generated-library installer and the
// schema table it merges into, from one module instance so the test sees the
// merged result.
export { installGeneratedBlocks, portOptional } from '../src/block-library';
export { RUNNABLE } from '../src/block-defs';
