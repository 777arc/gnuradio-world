import { readFile } from 'node:fs/promises';

const read = relativePath => readFile(new URL(relativePath, import.meta.url), 'utf8');

export const [
  mainSource,
  bootstrapSource,
  blockDefsSource,
  blockLibrarySource,
  validationSource,
  recordingCatalogSource,
  exampleCatalogSource,
  debugPanelSource,
  aiPanelSource,
  appChromeSource,
  canvasRendererSource,
  canvasConnectionsSource,
  canvasGesturesSource,
  editorStateSource,
  examplePaletteSource,
  paletteTreeSource,
  propertyFieldsSource,
  propertiesDialogSource,
  recordingPaletteSource,
  recordingTabsSource,
  runSessionSource,
  variableEditorSource,
  validationUiSource,
  workspaceTabsSource,
  htmlSource,
  cssSource,
] = await Promise.all([
  read('../src/main.ts'),
  read('../src/bootstrap.ts'),
  read('../src/block-defs.ts'),
  read('../src/block-library.ts'),
  read('../src/validation.ts'),
  read('../src/recording-catalog.ts'),
  read('../src/example-catalog.ts'),
  read('../src/debug-panel.ts'),
  read('../src/ai/panel.ts'),
  read('../src/app-chrome.ts'),
  read('../src/canvas-renderer.ts'),
  read('../src/canvas-connections.ts'),
  read('../src/canvas-gestures.ts'),
  read('../src/editor-state.ts'),
  read('../src/example-palette.ts'),
  read('../src/palette-tree.ts'),
  read('../src/property-fields.ts'),
  read('../src/properties-dialog.ts'),
  read('../src/recording-palette.ts'),
  read('../src/recording-tabs.ts'),
  read('../src/run-session.ts'),
  read('../src/variable-editor.ts'),
  read('../src/validation-ui.ts'),
  read('../src/workspace-tabs.ts'),
  read('../index.html'),
  read('../src/editor.css'),
]);

// Static browser-wiring tests use this aggregate so moving a declaration among
// focused editor modules does not invalidate an otherwise unchanged contract.
export const editorSource = [
  mainSource,
  bootstrapSource,
  blockDefsSource,
  blockLibrarySource,
  validationSource,
  recordingCatalogSource,
  exampleCatalogSource,
  debugPanelSource,
  aiPanelSource,
  appChromeSource,
  canvasRendererSource,
  canvasConnectionsSource,
  canvasGesturesSource,
  editorStateSource,
  examplePaletteSource,
  paletteTreeSource,
  propertyFieldsSource,
  propertiesDialogSource,
  recordingPaletteSource,
  recordingTabsSource,
  runSessionSource,
  variableEditorSource,
  validationUiSource,
  workspaceTabsSource,
].join('\n');

export const markupSource = `${htmlSource}\n${cssSource}`;
