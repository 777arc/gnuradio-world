# Third-party source

- `nlohmann/json.hpp`: JSON for Modern C++ 3.11.3, single-header distribution
  (MIT). Its version and license notice are retained at the top of the header.

Keep vendored dependencies here rather than mixing them with runner-owned
sources under `src/`.
